# -*- coding: utf-8 -*-

class Post:

    def __init__(self, sender, theme, MSG, time, type_t) :
        self.sender = sender
        self.theme = theme
        self.MSG = MSG
        self.time = time
        self.type = type_t
